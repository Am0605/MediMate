import { View } from 'react-native';

// This is a placeholder screen that won't be rendered
// The actual FAB is handled by the custom tabBarButton
export default function AIFabScreen() {
  return <View />;
}